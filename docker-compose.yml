services:
  store:
    build: .
    environment:
      DATABASE_URL: postgresql+psycopg://postgres:postgres@host.docker.internal:5432/market_data
      ADMIN_DATABASE_URL: postgresql+psycopg://postgres:postgres@host.docker.internal:5432/market_data
      APP_PORT: 8081
      ADMIN_TOKEN: dev-token-change-in-production
    ports: ["8081:8081"]
    extra_hosts:
      - "host.docker.internal:host-gateway"
